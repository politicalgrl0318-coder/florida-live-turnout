{
  "name": "florida-live-turnout-workspace",
  "private": true,
  "scripts": {
    "build": "npm --prefix 305-DATA-GIRL-DASHBOARD run install:ci && npm --prefix 305-DATA-GIRL-DASHBOARD run build"
  }
}
